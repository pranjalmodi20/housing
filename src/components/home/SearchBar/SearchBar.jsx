import React from 'react';

// Search functionality is integrated into the Hero component.
// This component is kept as a placeholder for standalone search use.
const SearchBar = () => {
  return null;
};

export default SearchBar;
